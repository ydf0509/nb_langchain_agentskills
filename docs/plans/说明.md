

# 实现一个 nb_langchain_agentskills 的 pypi三方包

要求包含

skill__list_skills  列举所有avaliable skills，能发现深层级文件夹的skills (这个方法要求能重写，方便lc-agent的那个只勾选部分skills的过滤， )
skill__load_skill   （比langchain_agentskills要多增加skill的真实文件夹路径）
skill__read_content 读取skill下的任意文件内容，包括scripts referces文件夹等任意文件
skill__get_skill_file_tree (删除这个，不要这个，学langchain_agentskills 在load_skill时候一起返回)
skill__execute_script （这个希望是不学run_skill_script的入参，而是入参用 （skill_name,command,max_run_ms,working_directory）,更加的灵活）

要求像langchain_agentskills那样用middware方法添加这些工具

## 参考
"D:\codes\github_proj\langchain-skills-adapter-main"
"D:\codes\github_proj\langchain-agentskills-main"


## 定义在`site-packages/langchain_agentskills/tools/` 

工具名 入参 返回 `list_skills` 无 `- **name**: description` 列表 `load_skill` `skill_name: str` skill 的 markdown body + resources 列表 + scripts 列表 `read_skill_resource` `skill_name: str` ,`resource_name: str` resource 文件内容 `run_skill_script` `skill_name: str` ,`script_name: str` ,`script_args: list[str] | None = None` 脚本 stdout

要参考，但别强制抄袭。


## 工具详细说明
skill__list_skills  
skill__load_skill   （比langchain_agentskills要多增加让 agent知道某个skill的真实文件夹路径）
skill__read_content 读取skill下的任意文件内容，包括scripts referces文件夹等任意文件
skill__execute_script （这个希望是不学run_skill_script的入参，而是入参用 （skill_name,command,max_run_msworking_directory）,更加的灵活）  自动把 当前skill的文件夹路径和 skillxx/scripts 都加到pythonpath中